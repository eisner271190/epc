{
  "environment_variables": {
    "auth": {
      "AUTH_BACKEND_HOST": {
        "required": true,
        "default": null,
        "description": "Host del backend de autenticación"
      },
      "AUTH_TOKEN_EXCHANGE_PATH": {
        "required": false,
        "default": "AUTH_TOKEN_EXCHANGE_PATH_NOT_SET",
        "description": "Path para intercambio de código por tokens"
      },
      "AUTH_LOGOUT_PATH": {
        "required": false,
        "default": "AUTH_LOGOUT_PATH_NOT_SET",
        "description": "Path para logout"
      },
      "AUTH_COGNITO_DOMAIN": {
        "required": true,
        "default": null,
        "description": "Domain de Cognito Hosted UI"
      },
      "AUTH_CLIENT_ID": {
        "required": true,
        "default": null,
        "description": "Client ID de la app en Cognito"
      },
      "AUTH_REDIRECT_URI": {
        "required": false,
        "default": "AUTH_REDIRECT_URI_NOT_SET",
        "description": "Redirect URI de la app (móvil)"
      },
      "AUTH_REDIRECT_URI_WEB": {
        "required": false,
        "default": null,
        "description": "Redirect URI para web"
      },
      "AUTH_SCOPES": {
        "required": false,
        "default": "openid email profile",
        "description": "Scopes solicitados"
      },
      "AUTH_IDENTITY_PROVIDER": {
        "required": false,
        "default": "Google",
        "description": "Identity Provider"
      }
    },
    "ai": {
      "PROMPT": {
        "required": true,
        "default": null,
        "description": "Template de prompt para generación de quizzes"
      },
      "API_KEY": {
        "required": false,
        "default": null,
        "description": "API Key para OpenAI"
      }
    },
    "theme": {
      "THEME_STYLE": {
        "required": false,
        "default": "chatgpt",
        "description": "Estilo de tema (chatgpt o md3)"
      }
    }
  }
}
