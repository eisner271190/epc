// Placeholder removed: legacy export deleted because `generator_quiz.dart`
// no longer exists in this path. Use `GeneratorQuiz` from
// `features/quiz/application/generator_quiz.dart` or the
// `AiServiceAdapter` via `ComponentRegistry`.
